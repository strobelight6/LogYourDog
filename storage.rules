rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Emulator: allow all reads/writes. Tighten these for production.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
